name: Compile the job at the scheduled Time

on:
  push:
    branches:
      - master  
env:
  DOTNET_VERSION: '6.0.x'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@master

      - name: setup dotnet
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
               
      - name: download dependency
        run: dotnet restore

      - name: build app
        run: dotnet build -c Release --no-restore

      - name: Send some mail
        uses: ChinmayMahajan123456789/build-job@master
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: chinmay.mahajan@amla.io
          password: chinmay@123
          subject: Workflow finished
          to: chinmay.mahajan@amla.io
          from: chinmay.mahajan2794@gmail.com
          body: Job completed ${{job.status}}
          html_body: file:/README.md
          
          
          



 
    


  



  